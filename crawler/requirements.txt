curl_cffi>=0.7
scrapling>=0.2
camoufox[geoip]>=0.4
# Scrapling's StealthyFetcher defaults to a patchright (patched Playwright) backend
# rather than camoufox unless told otherwise — confirmed live: "No module named
# 'patchright'" when it wasn't installed. Both need a post-install browser binary
# fetch step (see pythonFetch.ts ensurePythonEnv).
patchright>=1.40
