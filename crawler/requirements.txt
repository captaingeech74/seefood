curl_cffi>=0.7
camoufox[geoip]>=0.4
# scrapling's StealthyFetcher pulls in optional extras (patchright, msgspec, etc.)
# that a plain `pip install scrapling` doesn't include — confirmed live twice
# (missing patchright, then missing msgspec after adding patchright by hand).
# Installing every extra avoids further one-by-one whack-a-mole on the next
# scrapling release. Both patchright and camoufox need a post-install browser
# binary fetch step (see pythonFetch.ts ensurePythonEnv).
scrapling[all]>=0.2
patchright>=1.40
# Used only as the final unfamiliar-page interpreter in website acquisition V2.
# Pin the reviewed release because its browser and server dependencies move fast.
crawl4ai==0.9.2
# Official Overture downloader used by the versioned metro backbone sync.
overturemaps>=1.0.1,<2
