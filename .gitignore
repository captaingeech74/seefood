node_modules/
.next/
.env.local
.env*.local
.claude/settings.local.json
tsconfig.tsbuildinfo
.vercel

# founder-private, never commit
SEEFOOD_URL_STRATEGY.md

# crawler Python venv (self-installed by npm run crawl, machine-specific)
crawler/.venv/
__pycache__/
# downloaded DoorDash sitemap cache (~18MB/state, refetched every 24h)
crawler/.cache/
ORCHESTRATOR_HANDOFF.md
.DS_Store

# Track B nightly crawler logs (Kyle's Mac, machine-local)
logs/

# SeeFood DataLab raw samples and generated artifacts stay local.
data-lab/artifacts/
data-lab/raw/
data-lab/tmp/

# Kyle's own fixture photos (personal files, not app source)
fixture-photos/inbox/*
fixture-photos/processed/*
!fixture-photos/inbox/.gitkeep
!fixture-photos/processed/.gitkeep
